# Copy this instruction to the development agent

Use the attached `animal-farm-visual-lock` pack to align the current Animal Farm Android implementation and all future app pages to the owner-approved visual family. First read START-HERE.md and AGENT-INSTRUCTIONS.md in full, then the design system, page patterns, migration gates, exact locked HTML and affected registry entries. Do not infer the design from the current UI or later generated images.

Management must use A / Control room. Worker must use D / Work board with C's Review & decide carousel inside the selected work stage. Login must show Animal Farm centered above the original animals and credential fields with centered Sign in. Use the supplied family artwork and optional individual profile photos. Keep the home Settings gear bottom-right and Theme-only. Remove unnecessary helper text while preserving safety and recovery information.

Recover the actual active repository/branch/HEAD and preserve dirty work. Install this pack under `docs/ux/animal-farm-visual-lock/`, merge its AGENTS addendum and update contradictory visual authority references without changing domain or architecture rules. Run its integrity checker. Reconcile the live registry against all 545 supplied IDs (537 base plus eight role variants); do not drop variants to match the stale source footer. Resolve additional feature/route gaps explicitly.

Before broad restyling, implement and obtain native acceptance for the shared theme/components and reference family: login, management, worker, species navigator, profile, capture, safety, list and tablet/outdoor. Preserve Room/outbox/RPC/RLS/search/authorization behavior. Replace existing presentation in small tested slices and then complete remaining page contracts using the assigned patterns and module rules. The per-ID recipes are a starting point requiring field/action/permission review, not already-approved full screen specifications.

For each slice: declare screen/feature IDs; complete a contract; test the user job; render the state/device/font/theme matrix; compare native screenshots; obtain independent baseline approval; record evidence at the tested commit; update status truthfully. Never rewrite a golden or authority to make your output pass, bypass a gate, manufacture data, or claim feature/MVP completion from visual work alone. Finish by reporting verified results, open gates and the next registered slice. Do not claim impossible anti-drift guarantees: protect the reference/golden/registry files and require reviewed CI checks.
