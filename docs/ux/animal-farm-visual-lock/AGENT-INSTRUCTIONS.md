# Instruction to the development agent

## Mission and scope

Align the already implemented Animal Farm Android app and all future app surfaces to the owner-locked A management / D+C worker visual family. Preserve and finish the full quantum feature scope. This is a presentation migration over working domain contracts, not permission to replace the architecture, reduce MVP scope, invent clinical rules, create fake data, or treat a prototype as a working feature.

Read this entire pack before coding. Inspect the exact locked HTML visually and inspect its actual CSS/handlers as data. Read `references/HOME_DESIGN_LOCK.md`, `references/SCREEN_DESIGN_STANDARD.md`, `DESIGN-SYSTEM.md`, `PAGE-PATTERNS.md`, `MIGRATION-AND-GATES.md`, the affected page-alignment entries and the relevant feature specifications. Read the target repository's AGENTS.md and current product truth. Do not reconstruct the design from chat memory or a generated screenshot.

## Authority and reconciliation

1. Explicit owner decisions, including this request to apply the locked visual family across all app pages, govern presentation.
2. The supplied `references/animal-farm-locked-homes.html` and the original home decision govern the selected home compositions and animal identity treatment. The screenshot with a geometric goat was a section/layout reference; the subsequent explicit animal-lineup decision governs final imagery.
3. This pack translates the lock into app-wide rules. Its proposed page recipes, specialist variants and native adaptations must be reviewed before certification; they are not historical owner approvals.
4. Current project truth, feature contracts, quantum IDs and architecture continue to govern scope, permissions and behavior. An attractive design never grants a role permission or changes a task transition.
5. Existing `FARM_OS_VISUAL_AUTHORITY.md`, role dashboard contract, visual reference manifest and quantum document intensity descriptions must be amended where they contradict the recovered owner lock. Their feature inventory remains valuable. A comment claiming “locked session lineage” is not evidence of a visual match.
6. Existing implementation, later generated mockups, external themes, the unselected B view and standalone C home are not substitute authorities. The motor-spares adaptation discussed separately does not alter Animal Farm branding.

Do not delete earlier authorities. Record an explicit owner-decision amendment, point them to the new authority, preserve their historical context, and update conflicting always-on rules so agents do not receive two incompatible instructions.

## Non-negotiable presentation rules

- Display **Animal Farm** to users. Internal Farm OS package, database and repository names need not change.
- Management retains A: compact farm/date context, priority attention surface, work/resource summaries and module navigation. Do not turn the home into a Today board or uniform feature-description cards.
- Worker retains D's day/stage structure and useful work areas, with C's rich image-led task carousel inside the selected stage. Keep **Review & decide**. Never interpret that label as clinical/managerial approval authority.
- Primary actions are real commands or exact owning-route entry points. “Record weight” must open the scoped weight capture, not a generic goat module with instructions to find it.
- Use the versioned login lineup and family sheet. No geometric redraw, emoji animal, unrelated stock animal, regenerated lookalike or replacement pastoral scene.
- Individual profile photo is optional. Show name/tag/species even when a photo exists; preserve fallback and privacy. Group, litter, cage, house and flock identities remain distinct.
- Use one governed theme and component library. No page-local raw color literals, new font families, unreviewed radii or layout systems. Material primitives may implement behavior, but their default styling is not the design authority.
- Home Settings stays at bottom right, outside long scrolling content and inside safe insets. The home gear opens only Theme. Full Settings/Admin remains separately reachable with authorization. Do not expose development state/role/style pickers in production.
- Remove redundant helper paragraphs and slogans. Retain essential units, warnings, field errors, permissions, provenance and local/sync receipts.
- No dummy business totals, named demo animals, dead buttons, fabricated charts or “coming soon” placeholders in a release flow. Test fixtures belong in previews/tests only.
- No feature is dropped to make a page simpler. Use hierarchy, progressive disclosure and owning routes rather than hiding work.

## Per-change working protocol

1. Record branch, HEAD, dirty files, affected feature IDs and exact screen IDs. Preserve others' changes.
2. Compare the live quantum registry to the supplied snapshot. Preserve all IDs. Register newly discovered routes, dialogs, role variants and system-only contracts before implementing them.
3. Read the per-ID recipe and module rules. Resolve every provisional mapping against actual fields, actions and navigation. Copy `templates/screen-contract.json` to the repository's screen-contract directory and complete it. A registry entry is not a complete screen brief.
4. Bind the page to approved components and semantic tokens. Explain deviations in a decision record before changing the shared design.
5. Preserve existing state/command adapters. Add failing-first tests for the actual job, permission boundaries and recovery paths.
6. Implement one coherent screen/feature slice. Render required native states; compare against the approved reference for that page family and the actual home references. Review the full workflow, not only the first frame.
7. Complete the evidence record, including real artifacts and tested commit. Get the appropriate independent approval. Update status only after evidence passes.
8. Report what changed, what passed, what remains unverified and the next registered slice. Do not claim the whole MVP from one successful dashboard.

If a feature lacks a command, permission contract, approved clinical content or data source, finish the safe presentation foundation but keep the feature blocked/unverified. Do not invent the missing contract. Continue other independent, authorized work.

## Architecture to preserve during restyling

Keep Room/offline writes, durable drafts, mutation identity, outbox, WorkManager, conflict/rejection handling, Supabase command/RPC boundaries, RLS, reconciliation, search projection and local fallback. No Compose direct business-table writes. Keep farm scoping and authorization through routes, cached media, exports and command handlers. Local save is not server acknowledgment. Starting/opening a task is not completion. Unknown/stale values are not zero.

Full MVP includes Goats, Rabbits, Sheep, Cattle, poultry kinds and all shared operations, health, resources, people, commercial, analytics, genetics, simulation, search, AI, reporting, administration and sync surfaces. The old 146-screen catalogue is supplementary workflow detail; it must not replace the larger quantum registry.

## What you may not self-approve

Do not replace reference images, rewrite goldens to match your output, lower thresholds, change evidence hashes, suppress route coverage, delete difficult screen IDs, edit the authority to justify drift, or mark your own visual evidence independently approved. Propose an explicit change for owner review instead. A test skip is an open gate, not a pass.
