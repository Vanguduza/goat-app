name: Actions smoke

on:
  pull_request:

concurrency:
  group: actions-smoke-${{ github.event.pull_request.head.ref || github.ref_name }}
  cancel-in-progress: true

jobs:
  smoke:
    runs-on: ubuntu-latest
    steps:
      - name: Runner available
        run: echo "GitHub Actions runner is available for Farm OS"
