package com.farmos.feature.ops

/** Deep-entry contract for existing health pages. Withdrawal detail is not invented here. */
enum class HealthEntryPage {
    DASHBOARD,
    TREATMENT,
    WITHDRAWALS,
    RECORD_OBSERVATION,
    VET_VISIT,
    LAB_RESULT,
}
